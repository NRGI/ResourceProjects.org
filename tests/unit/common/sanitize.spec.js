describe("sanitize FILTER", function() {
    beforeEach(module('app'));
    console.error('SANATIZE FILTER: NO TEST');
    // var $filter;
    //
    // var $sce;
    // //     codeFilter;
    // //
    // // it('should have a sanitize filter', inject(function ($filter) {
    // //     expect($filter('sanitize')).not.toBeNull();
    // // }));
    // beforeEach(inject(
    //     function (_$filter_) {
    //         $filter = _$filter_;
    //     }
    // ));
    //
    // it('sanitized incoming html', function (_$sce_) {
    //     $sce = _$sce_;
    //     console.log($sce);
    //     $sce.getTrustedHtml($filter('sanitize')('Andrea (Michoacán)')).should.be.equal('Andrea (Michoacán)');
    //     // $filter('zpad')('024',2).should.be.equal('24');
    //     // $filter('zpad')(1, 1).should.be.equal('1');
    //     // $filter('zpad')(1, 3).should.be.equal('001');
    //     // $filter('zpad')(1, 5).should.be.equal('00001');
    // });
    //
    // // it('testing sanitize filter', inject(function ($filter,_$sce_) {
    // //     $filter('addSpaces')('no_space').should.be.equal('no space');
    // //     $sce = _$sce_;
    // //     codeFilter = $filter('sanitize');
    // //     expect(
    // //         $sce.getTrustedHtml(
    // //             codeFilter('Andrea (Michoacán)')
    // //         )
    // //     ).should.be.equal("Andrea (Michoacán)");
    // //     expect($sce.getTrustedHtml(codeFilter('ἱερογλύφος'))).should.be.equal("ἱερογλύφος");
    // // }));
});